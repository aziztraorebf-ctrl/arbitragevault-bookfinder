feat: Phase 1 - Complete API endpoints and audit foundations

✅ Endpoints Implementation:
- GET /analyses (pagination, filters)  
- GET /analyses/top (ROI/velocity/balanced strategies)
- POST /analyses (validation + error handling)
- GET /batches (status, progress)
- PATCH /batches/{id}/status (state transitions)
- GET /keepa/test (stub for Phase 2)

✅ Infrastructure:
- UserRole enum added (fixes concurrent tests)
- Async SQLAlchemy config fixed (removed QueuePool)
- Structured logging simplified for Phase 1
- API schemas aligned with existing models

✅ Validation & Tests:
- Concurrent tests: 20 GET + 5 POST + 5 PATCH (100% success)
- Performance: pagination deep <250ms (avg 71ms)
- Security: .env.example, CORS dev, DEBUG=false validation
- Cascade delete: ondelete='CASCADE' confirmed

✅ Error Handling:
- POST /analyses: 422 validation errors
- PATCH /batches: 400 invalid status
- Proper HTTP status codes
- Structured error responses

Ready for Phase 1.4: Keepa Integration

🤖 Generated with [Memex](https://memex.tech)
Co-Authored-By: Memex <noreply@memex.tech>